/* eslint-disable no-console */

const assert = require('assert');

const ReactInstantSearch = require('react-instantsearch');

assert.ok(ReactInstantSearch);

console.log('react-instantsearch is valid CJS');
