function MySearchbox() {
  const placeholder = 'Search here';
  return (
    <SearchBox
      translations={{
        placeholder,
      }}
    />
  );
}

function MyRefinementList() {
  const placeholder = 'Search here';
  return (
    <RefinementList
      translations={{
        placeholder,
      }}
    />
  );
}
