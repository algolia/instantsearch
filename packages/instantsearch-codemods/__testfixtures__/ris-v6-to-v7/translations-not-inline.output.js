function MyBreadcrumb() {
  const translations = { rootLabel: 'Home' };
  return (
    <Breadcrumb
      /* TODO (Codemod generated): Follow the upgrade guide to rename the keys of the `translations` prop
      See https://www.algolia.com/doc/guides/building-search-ui/upgrade-guides/react/ */
      translations={translations}
    />
  );
}

export default MyBreadcrumb;
