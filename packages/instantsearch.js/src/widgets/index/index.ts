export { index as default } from 'instantsearch-core';

export type {
  IndexInitOptions,
  IndexRenderOptions,
  IndexWidget,
  IndexWidgetDescription,
  IndexWidgetParams,
} from 'instantsearch-core';
