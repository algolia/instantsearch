<% for (const [propName, meta] of Object.entries(props)) { %>
  <div>
    <strong><%= propName %></strong>
    <% if (meta.description) { %>
      <p><%- meta.description %></p>
    <% } %>
  </div>
<% } %>
